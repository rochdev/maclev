version: '2'
services:
  web:
    build: .
    command: ./node_modules/.bin/gulp
    volumes:
      - ./lib:/usr/src/app/lib
      - ./migrations:/usr/src/app/migrations
  postgres:
    ports:
      - 5432:5432
